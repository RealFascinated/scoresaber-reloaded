export type HttpSiraStatus_GameData = {
  /**
   * The version that the game is running.
   */
  gameVersion: string;

  /**
   * The current characteristic of the game.
   */
  mode: string;

  /**
   * The version of the plugin.
   */
  pluginVersion: string;

  /**
   * The current scene in the game.
   */
  scene: string;
};
