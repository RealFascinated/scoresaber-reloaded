/** Delay before search/query API requests after typing stops. */
export const DEBOUNCE_MS_SEARCH = 300;

/** Delay before filter-driven list refetches (category, stars, toggles). */
export const DEBOUNCE_MS_FILTER = 100;

/** Delay before persisting form or settings input. */
export const DEBOUNCE_MS_FORM = 500;

/** Delay before recomputing derived UI from slider input (e.g. charts). */
export const DEBOUNCE_MS_SLIDER = 300;
