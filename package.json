{
  "name": "scoresaber-reloaded",
  "version": "1.0.0",
  "workspaces": [
    "projects/*"
  ],
  "scripts": {
    "dev:website": "bun --filter 'website' dev",
    "dev:backend": "bun --filter 'backend' dev",
    "dev:common": "bun --filter '@ssr/common' dev",
    "dev": "bun --filter 'website' --filter 'backend' --filter '@ssr/common' dev",
    "prettier": "prettier --write ."
  },
  "author": "fascinated7",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "prettier": "^3.5.0",
    "superjson": "^2.2.2",
    "axios": "^1.7.9"
  }
}